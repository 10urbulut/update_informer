name: update_informer
description: Updating for informally.
version: 1.0.2
homepage: https://github.com/10urbulut/update_informer


environment:
  sdk: '>=3.0.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  dio: ^5.3.2
  flutter:
    sdk: flutter

  json_annotation: ^4.8.1
  package_info_plus: ^4.1.0
  provider: ^6.0.5
  store_redirect: ^2.0.1



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.6
  json_serializable: ^6.7.1


  # dart pub publish --dry-run
  # do version upgrade
  # dart pub publish